# Debian 旁路由项目运行规则

## 入口检查

处理本项目时先判断任务属于：生产故障、项目开发、配置审计、发布或文档。读取与任务直接相关的当前文件和实时状态，加载 `linux-router-operations`；调试异常时同时加载 `systematic-debugging`。

## 权威来源

实时系统状态与生产配置优先于项目文档、外部记忆和历史会话。项目实现以当前代码、模板、测试和正式文档为准。外部记忆与历史会话只提供线索，不得覆盖原始来源。

## 生产变更硬约束

- 未经明确授权不得修改主路由或全网 DHCP。
- 修改前保留管理路径并创建可执行回滚。
- Mihomo、MosDNS、AdGuard Home、nftables 配置必须先使用原生校验。
- TProxy 作为事务处理：监听器、nftables、mark、`ip rule`、路由表、启动和清理必须一致。
- DNS 公网上游必须加密；故障时 fail closed，禁止静默回退公网 UDP/TCP 53。
- 服务 `active` 不是完成标准，必须验证真实 DNS、TCP/UDP 和目标业务路径。
- 失败时回滚到已验证状态，不得用推测结果冒充执行结果。

## 节点策略

手动指定的常用节点健康时保持不变；仅故障时使用备用，恢复后自动切回。所有代理不可用时允许临时 DIRECT 保底，但 DIRECT 不得参与 URLTest 延迟竞争。

## 数据与隐私

禁止把订阅 URL、Token、密码、节点凭据、API Key、客户端流量历史或机器身份写入仓库、文档、记忆、日志摘录和测试夹具。配置示例必须脱敏和参数化。

## 工作方式

将大型工作拆成可独立验收的阶段。用户说停止时立即停止新写入，检查后台进程，保留现有工件，等待明确恢复。每阶段必须区分原型、已实现、已验证和可发布状态。

## 记忆治理

遵循 `docs/authority-and-memory.md`：用户偏好进核心记忆，长期事实按需进外部记忆，项目事实进项目文件，可复用流程进 Skill，强制要求进程序校验和运行时规则，临时进度留在会话或任务文件。